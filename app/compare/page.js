import CandidateComparePage from '@/src/views/recruiter/CandidateCompare';

export const metadata = {
  title: 'Compare Candidates — Shadow Coder',
  description: 'Compare candidate coding sessions side by side',
};

export default function Compare() {
  return <CandidateComparePage />;
}
