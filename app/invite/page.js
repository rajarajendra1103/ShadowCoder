import SendInvitePage from '@/src/views/recruiter/SendInvite';

export const metadata = {
  title: 'Send Invite — Shadow Coder',
  description: 'Invite a candidate to a coding session',
};

export default function Invite() {
  return <SendInvitePage />;
}
